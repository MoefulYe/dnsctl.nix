{
  "name": "dnsctl-nix",
  "private": true,
  "type": "module",
  "dependencies": {
    "cloudflare": "^4.0.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "pnpm run build",
    "typecheck": "tsc --noEmit"
  }
}
